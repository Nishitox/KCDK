Move Blocks / MB

ブロックをコマンドが実行された位置まで下げます。
function kcdk:plugin/mb/get でコマンドを入手します。

導入方法
mb.zipを解凍して kcdk:function/plugin/ に入れてください。

設定方法
kcdk:function/plugin/mb/score/return/blacklist/ で操作を行わないブロックを指定できます。